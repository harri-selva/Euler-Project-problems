def compute():
    number = 2 ** 1000
    digit_sum = 0

    # Extract digits one by one without converting to string
    while number > 0:
        digit_sum += number % 10  # Add the last digit
        number //= 10             # Remove the last digit

    return str(digit_sum)

if __name__ == "__main__":
    print(compute())